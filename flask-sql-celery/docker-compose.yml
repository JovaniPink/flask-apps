services:
  broker-rabbitmq:
    image: "rabbitmq:4.3-management@sha256:ffd1b50c522ad20172ffd6716a2f41db375c7269560c8f3fb9a694e210ef0852"
    environment:
      RABBITMQ_DEFAULT_USER: rabbit_user
      RABBITMQ_DEFAULT_PASS: rabbit_password
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "-q", "ping"]
      interval: 5s
      timeout: 5s
      retries: 12
  db-postgres:
    image: "postgres:18.6@sha256:4ef4dbc939d61acea57712655ddb4b4ab27419c913f94cca0cd57cb3ea3c2280"
    environment:
      POSTGRES_DB: flask_deploy
      POSTGRES_USER: db_user
      POSTGRES_PASSWORD: db_password
    volumes:
      - pgdata18:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U db_user -d flask_deploy"]
      interval: 5s
      timeout: 5s
      retries: 12
  migration:
    build: .
    environment:
      APP_ENV: Production
    command: flask --app app:create_app init-db
    depends_on:
      db-postgres:
        condition: service_healthy
  api:
    build: .
    ports:
      - "5000:5000"
    environment:
      APP_ENV: Production
    depends_on:
      broker-rabbitmq:
        condition: service_healthy
      migration:
        condition: service_completed_successfully
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:5000/healthz')"]
      interval: 5s
      timeout: 5s
      retries: 12
  api-worker:
    build: .
    command: celery --app tasks.celery worker --loglevel=info
    environment:
      APP_ENV: Production
    depends_on:
      broker-rabbitmq:
        condition: service_healthy
      migration:
        condition: service_completed_successfully
  api-beat:
    build: .
    command: celery --app tasks.celery beat --loglevel=info
    environment:
      APP_ENV: Production
    depends_on:
      broker-rabbitmq:
        condition: service_healthy
      migration:
        condition: service_completed_successfully

volumes:
  pgdata18:
